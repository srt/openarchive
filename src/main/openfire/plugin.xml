<?xml version="1.0" encoding="UTF-8"?>
<plugin>

    <!-- Main plugin class -->
    <class>com.reucon.openfire.plugin.archive.ArchivePlugin</class>
    
    <!-- Plugin meta-data -->
    <name>${pom.name}</name>
    <description>${pom.description}</description>
    <author>${pom.organization.name}</author>
    <version>${pom.version}</version>
    <date>${openfire-plugin.build.date}</date>
    <minServerVersion>3.3.0</minServerVersion>
    <databaseKey>archiver</databaseKey>
    <databaseVersion>1</databaseVersion>
    <licenseType>gpl</licenseType>
    
    <!-- Admin console meta-data -->
    <adminconsole>              

        <tab id="tab-server">
            <sidebar id="sidebar-server-settings">
               <item id="archiver-settings" name="${archive.settings.title}"
                   url="archiver-settings.jsp"
                   description="${archive.settings.description}" />
               <item id="archiver-search" name="${archive.settings.title}"
                   url="archiver-search.jsp"
                   description="${archive.search.description}" />
            </sidebar>

        </tab>

    </adminconsole>
</plugin>
